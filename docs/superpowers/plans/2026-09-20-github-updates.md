# GitHub Releases 更新接入计划

**已确认：** 用户没有 Windows 代码签名证书，也没有现有 Ed25519 清单密钥，授权按新密钥方案准备。本地接入已验收，用户随后授权配置 GitHub Secret、启用更新源、提交推送并沿用 v26.09.01 重发。

- [x] 修正更新窗口状态文案，增加双渠道 GitHub 下载页按钮，保留安装能力门控。
- [x] 没有安装身份时只做显示版本比较，不能创建下载/执行资格；预览同版本不误报最新构建。
- [x] 生成仓库外 Ed25519 私钥，版本管理只包含公钥；CMake 可选编入生产更新源。
- [x] 新增签名工具、GitHub 清单发布/续签工作流与离线测试。
- [x] CI 构建受仓库变量控制，并补齐签名测试依赖。
- [x] 编写上线步骤、已有版本的首次手动升级要求和已知限制。
- [x] 完整构建、82 项 CTest、53 项 Python 测试、Python → C++ 实际验签和独立代码审查；审查问题已修复复核。

**部署推进中：** 备份旧 v26.09.01 产物，修正 Windows 测试重试配置与 macOS 临时镜像卸载失败处理；配置 Secret/变量后重发同版本并验签线上清单。旧 v26.09.01 用户需手动重装。首次上线步骤见 `docs/development/GITHUB_UPDATES.md`。

**推迟的自动安装工作：** 等 Windows 签名方案具备后，再接通发布者校验、安装身份、生产执行器和真实 Windows 升级验收。本轮不实施、不宣称已完成自动安装。
