# UI 验收收尾计划

**目标：** 同步当前交付状态，并提供 Windows 同环境旧新版大文件测量证据。

**架构：** 不修改生产 UI 或数据算法。沿用真实窗口集成测试，基线使用 UI 重构前的 248fc7c5，新版使用 45f7fd2d 的应用代码；测量夹具可单独适配旧接口。分别使用各提交锁定的框架版本，因此结论针对整体版本，不单独归因于 UI。

**技术栈：** Windows、Qt 6.11.0、MSVC 18、CMake、Release，关闭 LTO 和 Hyperscan。

**规格：** [UI 重构路线](2026-09-07-zzpuretools-ui-roadmap.md) 第五阶段；用户批准先整理状态、再进行性能对照。

## 约束

- 保留用户 README、图片及其他工作区；不推送，不自动合并。
- Linux/macOS 测试继续暂停；人工多屏和 DPI 验收保持未完成。
- 不用一个性能样本宣称无退化，不把重构后的版本误标为重构前基线。
- 旧新版使用相同日志内容、窗口尺寸和搜索条件；测试串行运行，注明热缓存及进程峰值口径。

## 执行步骤

- [x] 更新总路线与阶段五的当前状态，保留历史结果但标明日期。
- [x] 检查并构建旧版基准适配器；新版复用大日志集成测试并增加滚动测量。
- [x] 两边各热身一次，交替测量五次，保留原始日志、退出码、环境及摘要；为核实波动另复测一组。
- [x] 记录打开、搜索、滚动、进程峰值内存的结果与限制；不能完成的指标不勾选。
- [x] 验证测量夹具和运行器，分步中文提交；人工多屏及跨平台仍单独列出。

## 执行结果

2026-09-14 补充：用户已确认人工多屏、缩放、拖动与吸附及设置窗口 150% 缩放正常，相关功能验收通过。随后批准线性合入 master 并编译；下文未合并状态与旧提交号保留为当时记录。滚动差异进入单独诊断，不因 UI 功能验收通过而抹去该测量信号。

[性能报告与原始样本](../../performance/2026-09-14/README.md) 已整理。两组共 24 次调用（含热身）全部成功；新版完整 Windows 原生界面测试 48 项通过，独立审查确认测量段一致。状态整理提交 194a5d00，工具提交 9265f5b2。

打开、搜索无稳定回退证据，进程峰值变化方向不一致；两组均观察到新版离散滚动总耗时略高，保留为待剖析信号。本任务完成测量和记录，不代表性能全面验收通过。修改位于 codex/ui-acceptance-followup，未合并、未推送。
